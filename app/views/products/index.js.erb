$('.my-posts').append('<%= j render @products %>');
<% if @products.next_page %>
  $('.pagination').replaceWith('<%= j paginate @products %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
